models:
  - type: main
    engine: anthropic
    model: claude-sonnet-4-20250514

rails:
  input:
    flows:
      - check prompt injection
  output:
    flows:
      - check hardcoded secrets
      - check unsafe file paths
